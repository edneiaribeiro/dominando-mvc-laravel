<div class="col-4">
    <div class="card">
        <img height="200" src="{{ $imagem }}" alt="Parliament Hill" alt="{{ $titulo }}">
        <div class="card-body">
            <h5 class="card-title">{{ $titulo }}</h5>
            <p class="card-text">{{ $descricao }}</p>
            <a class="btn btn-block btn-primary" href="{{ $url }}">Ver mais</a>
        </div>
    </div>
</div>